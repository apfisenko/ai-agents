# План разработки (итерации)

## Прогресс

Обновляйте колонку **Статус** и чекбоксы ниже после каждой итерации.

| № | Итерация | Статус |
|---|----------|--------|
| 1 | Каркас, конфигурация, старт в Telegram | 📋 Planned |
| 2 | Диалог с моделью и памятью в процессе | 📋 Planned |
| 3 | Упаковка и запуск в Docker | 📋 Planned |

### Легенда статусов

| Иконка | Статус | Значение |
|--------|--------|----------|
| 📋 | Planned | Запланирован |
| 🚧 | In Progress | В работе |
| ✅ | Done | Завершён |

---

## Итерация 1 — Каркас, конфигурация, старт в Telegram

**Проверка:** бот запускается; в Telegram отрабатывает `/start`; нетекстовые сообщения ведут себя по единому правилу (как в vision); при неполной конфигурации приложение не стартует с понятной ошибкой; логи идут в консоль.

- [ ] Репозиторий: зависимости, точка входа, long polling
- [ ] Загрузка настроек из окружения и пример `.env.example`
- [ ] Логирование (уровень из env, без секретов в логах)
- [ ] Команда `/start` и короткое приветствие
- [ ] Единая политика для нетекстовых сообщений

---

## Итерация 2 — Диалог с моделью и памятью в процессе

**Проверка:** текстовое сообщение → ответ от LLM с учётом системного промпта из файла; несколько реплик подряд сохраняют контекст до перезапуска; при сбое API пользователь видит короткое нейтральное сообщение, без утечки деталей.

- [ ] Файл системного промпта и подключение к запросу
- [ ] Хранилище истории по `chat_id` только в памяти
- [ ] Клиент OpenRouter (совместимый вызов chat completions)
- [ ] Связка: новое сообщение → история → ответ → обновление истории
- [ ] Обработка ошибок LLM для пользователя и логов

---

## Итерация 3 — Упаковка и запуск в Docker

**Проверка:** `docker compose` поднимает сервис; бот ведёт себя как при локальном `uv run`; переменные окружения подхватываются; те же сценарии из итераций 1–2 проходят в контейнере.

- [ ] Dockerfile (Python 3.11, uv)
- [ ] `docker-compose.yml` с одним сервисом приложения
- [ ] Make-цели и дублирующие команды или скрипты для PowerShell
